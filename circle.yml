machine:
  python:
    version: 3.5.2

dependencies:
  pre:
    - sudo pip install --upgrade pip
    - sudo pip install -r requirements/test.txt

test:
  override:
    - nosetests -w test/domain/cosine_similarity